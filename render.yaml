services:
  - type: web
    name: sales-commission-backend
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    # Opcjonalne - jeśli używasz email
    # envVars:
    #   - key: SMTP_USER
    #     sync: false
    #   - key: SMTP_PASS
    #     sync: false


